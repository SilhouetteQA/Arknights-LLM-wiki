# 会话总结 — 2026-06-29-1 (技术债务修复 + CASUAL→百科风格)

## 任务

上回闪断后，使用 brooks:debt 扫描发现 26 项技术债务。按优先级修复 8 项（P×S 3-9），另完成 persona 命名修正和主线章节映射。

## 修复清单

### Critical (P×S=7-9)
- **A1 [P×S=9]** FAISS 嵌入失败静默回退随机向量 → 改为明确 RuntimeError
  - `vector_index.py`: 移除 `build_faiss_index` 和 `semantic_search` 中两处 `except Exception: np.random.randn`
  - `test_vector_index.py`: 3 个测试改用 `monkeypatch` mock `_encode_texts`

### Scheduled (P×S=4-6)
- **K1+K2 [P×S=4,4]** 提示词 4 处重复 → 提取 5 个共享规则块 (`_SOURCE_FIDELITY_RULES`, `_SOURCE_FIDELITY_EXTENDED`, `_LOGICAL_ORGANIZATION_NARRATIVE`, `_LOGICAL_ORGANIZATION_SYNTHESIS`, `_BAN_LIST`)
  - `prompts.py`: QA_SYSTEM_PROMPT/AGENT_SYSTEM_PROMPT/SYNTHESIS_PROMPT 均引用共享块
  - `simple_search.py`: build_answer_prompt 复用共享块
- **C1 [P×S=6]** `search_and_collect` 108 行单体函数 → 拆分为 `_resolve_chapter_context` + `_collect_structured_sources` + `_collect_semantic_fallback`
- **D1 [P×S=6]** 5 个 Store 类无共享基类 → `_BaseStore` + `SearchResult` TypedDict，统一 `__init__` 模式
- **A2 [P×S=4]** TOOL_DEFINITIONS 手动 3 处同步 → `@tool` 装饰器注册 + 自动生成 `TOOL_DEFINITIONS` + `TOOL_EXECUTORS`
- **C4+C5 [P×S=4+2]** router.py 函数职责混杂 → 提取 `_llm_intent_rewrite` + `_make_intent_result`/`_make_complexity_result` 工厂函数
- **C2 [P×S=4]** `build_chunk_map` 3 块重复遍历 → concepts/factions/locations 合并为单循环

### Monitored (P×S=1-3)
- **A3 [P×S=3]** `completion_fns.py` 模块导入副作用 → `_ensure_evals_api_key()` 仅在 `__init__` 调用

### 额外修复
- **Persona 命名**: 4 处 "CASUAL persona" 改为 "百科风格"（实际内容为严肃百科，非闲聊风格）
- **主线章节映射**: identity_map.json 添加 `主线/主线剧情/主线故事/正传 → 主线章节`，创建概念页 `主线章节.md`（18 章），重建 FAISS 索引

## 修改文件

```
arknights_wiki/agent/prompts.py          # 共享规则块 + persona 命名
arknights_wiki/agent/simple_search.py    # 单体函数拆分 + 共享块引用 + persona
arknights_wiki/agent/router.py           # LLM 兜底提取 + 工厂函数
arknights_wiki/agent/retrieval.py         # _BaseStore + SearchResult TypedDict
arknights_wiki/agent/tools.py            # @tool 装饰器注册
arknights_wiki/agent/vector_index.py     # A1 静默回退移除 + C2 循环合并
arknights_wiki/eval/completion_fns.py    # A3 副作用移除
config/identity_map.json                 # 主线章节别名
data/extractions/v3_wiki/concepts/主线章节.md  # 新概念页
tests/agent/test_vector_index.py         # mock _encode_texts
```

## 代码基线

| 分支 | 提交 | 说明 |
|------|------|------|
| fix/tech-debt-agent-priority | 542c00f | 8 项债务修复 |
| feature/langgraph-agent | 94d5063 | 合并修复 + persona + 主线映射 |

测试: **76/76 passed**

## 遗留问题

1. 前端测试 "主线第几章" 等常见问题 — 需重启服务器加载新索引
2. QA 日志已清空，下次测试可收集新的问答对
3. K6（双检索路径）为有意设计（simple/complex 分层路由），标记为 intentional
4. M4（line_range 三种格式）在 extraction 模块，未在本次 agent 范围修复
5. 低优先级债务（M1/M3/D3/D6 等 P×S 1-3）留待后续

## 下一步

重启服务器（加载新索引） → 前端手动测试 → 评估器重新跑基线 → 根据结果继续调优
