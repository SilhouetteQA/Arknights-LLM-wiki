# 2026-08-19 工作日志

## W4 增强与三路对比（13:00-）

- 用户决策：Planner 崩溃直接进 ReAct 兜底（ARKNIGHTS_PLANNER_FALLBACK 默认开）+ 任务级 ReAct 混合（ARKNIGHTS_PLANNER_TASK_REACT）
- 实现：should_fallback_to_react（空证据/弱证据≥60% → fallback）、build_planner_graph 条件边 execute→(fallback→agent⇄tools→synthesize | continue→synthesize)；_execute_task_react 子 ReAct 循环（≤3 步）；测试 6 个新增
- 自测 Q5 重测：LLM 规划质量好时（27 工具）fallback 不触发，回答完整——LLM 规划波动是主因，fallback 是保险丝
- ⚠️ 上轮 commit 因 rm -rf 被安全策略拦截未执行（trash 失败）——W4 变更仍在工作区（24 个变更）
- 用户要求：先三路由（react/planner/planner_task_react）× 两数据集（benchmark 10 题六指标 + 自测 5 问质量评估）对比，查看结果后再提交
- 脚本：scripts/w4_three_mode_compare.py（总控）、w4_user_test.py 改造支持 3 模式；输出 w4_cmp_{mode}/report_v1.md + w4_three_mode_report.md

### W4 提交完成（14:40-14:50）

- **三路对比结论**（同环境 benchmark 10 题）：ReAct 0.942 > Planner 0.903 > task_react 0.758（工具选择 0.2 结构性缺陷）
- **最终决策（用户，质量优先）**：默认 `ARKNIGHTS_AGENT_MODE=react`；Planner 保留选项（=planner 带崩溃 fallback）；任务级 ReAct 留实验开关默认关；ReAct 步数维持 8
- 并行执行增强：execute_task_graph/execute_task_react_graph 分层并行（≤4 并发，contextvars 传播），测试 +3
- **提交 `644c551`**（W4 全量：planner/fallback/task_react/并行/直连修复/三路对比产物），全量 542 passed
- P0 全部完成（W0-W4）✅，下一步 W10 收尾（简历定位+文档+演示）
