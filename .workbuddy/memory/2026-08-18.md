# 2026-08-18 工作日志

## W1 Observability/Tracing 完成（Langfuse v4 本地 Docker + SDK 4.14）

- **用户决策**：本地 Docker 部署 Langfuse v4 + Langfuse Python SDK 埋点（@observe）
- **部署**：`docker/langfuse/docker-compose.yml`（6 容器），headless 初始化项目 `arknights-wiki-main`
  - UI: http://localhost:3000（admin@arknights-wiki.local / ArknightsWiki2026!）
  - 启动: `cd docker/langfuse && docker compose up -d`
  - 密钥: `docker/langfuse/.env`（LANGFUSE_PUBLIC_KEY/SECRET_KEY/BASE_URL）
- **代码**：新包 `arknights_wiki/observability/`（client/decorators/schema），可开关 traced 装饰器（LANGFUSE 三键 + ARKNIGHTS_TRACING 开关，关闭态零开销）
- **埋点**：chat_completion→llm_call(generation)、router→span、simple_search/retrieval/answer_generation、graph 的 agent_call_model/tool_call/synthesize、server 的 chat_request 根、eval runner 每题 trace（benchmark_id）
- **验收**：simple + complex 真实问答 trace 树完整；ClickHouse events_full 确认 usage/cost/model 记录
- **脚本**：scripts/trace_smoke.py（冒烟）、scripts/trace_cost_summary.py（ClickHouse 成本聚合 → output/observability/cost_summary.md）
- **测试**：tests/observability/ 16 tests；全量 410 passed（4 预存失败：3 stats + 1 worldbuilding 数据文件测试）

## 踩坑记录（可复用）

1. Docker Hub 直连不通 → daemon.json 配 registry-mirrors（docker.1ms.run/daocloud/dockerproxy.net 实测可用）
2. Langfuse compose：DATABASE_URL 必须与 POSTGRES_PASSWORD 一致；S3 密钥必须与 MINIO_ROOT_PASSWORD 一致
3. SDK v4：usage/cost 只能用 update_current_generation（update_current_span 无此参数）
4. v4 events_only：observations 在 ClickHouse，v2 列表 API 是 lightweight view（无 usage/cost）
5. eval 成本日志路径可用 ARKNIGHTS_COST_LOG 覆盖

## ⚠️ 遗留严重问题

- **项目 .git 目录消失**（会话早期 git status 正常且对象库损坏，后整个消失；工作区文件完整）。未提交任何 W1 改动。需用户决策：恢复备份 / 重新 git init
- `output/eval/cost_log.jsonl` 被进程硬锁（WinError 5，无法写入/重命名，ACL 正常），已用 ARKNIGHTS_COST_LOG 绕过
- `data/extractions/v3_seed_db_v2.json` 被 worldbuilding 测试改写（已 git checkout 恢复——但 git 已不可用，实际未能恢复）

## Observability Dashboard 可视化（下午追加）

- 独立 ECharts 可视化页（端口 8001，区别于 agent 8000）：`arknights_wiki/observability/dashboard.py` + `static/dashboard.html`
- 数据直连 Langfuse ClickHouse（clickhouse-connect 127.0.0.1:8123，凭据从 docker/langfuse/.env 读）
- API：/api/overview、/api/traces、/api/trace/{id}（span_id/parent_span_id 建树）
- 图表：指标卡×5、请求量&延迟时间序列、成本&Token 趋势、节点类型饼图、延迟直方图、trace 列表+树图弹窗
- 启动：`python -m arknights_wiki.observability.dashboard`（需 Langfuse 栈运行中）
- 踩坑：ClickHouse toFloat64OrZero 只接受 String；.env 读取路径 parents[2]；总成本从 GENERATION 聚合

### 用户反馈迭代（下午二轮）

- 节点类型分布 → **任务/工具调用分布**（横向条形图，按节点 name 聚合次数+成本，tooltip 显示 cost）
- 延迟直方图 → **最慢 Trace Top 10**（按端到端延迟排序的水平条形图，可定位慢请求）
- trace 树 → **聚合折叠**：同层同名节点合并 "name ×N"（延迟/成本累加），tool_call 用 metadata.tool 具体工具名（后端 trace_detail 用 `mapFromArrays(metadata_names, metadata_values)['tool']` 提取），46 节点 → 10 节点；树默认展开 2 级（initialTreeDepth:2），更深折叠可点击展开

### 双问题实测（下午三轮，清缓存后）

清空 ClickHouse events_full/events_core/observations/traces 后跑两个真实问题（开 trace）：

| 问题 | 路由 | LLM 调用 | 工具调用 | tokens in/out | 真实成本(ClickHouse) | run_direct 估算 |
|------|------|---------|---------|---------------|---------------------|----------------|
| 莱茵十杰分别是谁 | complex | 10 | 20 | 118,421/3,951 | ¥0.2684 | ¥0.0027 |
| 主线维多利亚篇章 | complex | 10 | 20 | 133,781/7,643 | ¥0.3287 | ¥0.0106 |

- 关键发现：**run_direct 的 `_estimate_llm_cost` 严重低估成本**（只按 answer 字符估算 output tokens、input=0），真实成本 95%+ 是 input tokens（Agent 多轮把工具结果喂给 LLM）。W1 trace 的 provided_cost_details 是精确值——估算 vs 精确差 ~100 倍，证明 W1 成本可观测价值
- LLM 调用逐次明细可查（model=deepseek-v4-flash-ga-260731，每次 input tokens 递增——检索上下文累积）
- 失败 trace 也会留痕（APIConnectionError 时 usage=0 的 llm_call 仍记录，n_llm 含它）

### 前端迭代（下午三轮）

1. **任务/工具分布**：按 name 聚合（横向条形），log 刻度（数量差异大时小值可见）
2. **移除"最慢 Trace Top10"**（用户判定无用），成本 Token 趋势改全宽，tokens in/out 改双折线（不再堆叠）
3. **trace 详情改为时间线视图**（替代 ECharts 树）：路由信息卡（问题/complexity/question_type/来源/实体，数据来自根 chat_request metadata）+ 节点时间线表（按 DFS 顺序、缩进表示嵌套、每节点耗时/模型/成本/tokens）+ 底部汇总
   - 关键发现：SDK v4 的 `update_current_span(metadata=...)` **不落 ClickHouse**（router span 的路由 metadata 丢失）——路由信息实际在**根 chat_request 创建时传入的 metadata**（eval runner 传了 complexity/question_type/source/benchmark_id），entities 需在创建时传
   - root 的 input 是 `{"question":...}` JSON 串，后端 trace_detail 解析取 question 文本

### Git 历史恢复与首次提交（下午收尾）

- **远程仓库确认**：github.com/SilhouetteQA/Arknights-LLM-wiki（main + feature/langgraph-agent），最后推送 2026-07-04，154 条提交
- **恢复方式**（零删除，用户否决过 rm -rf）：`git remote add origin` → `git fetch origin`（对象入 pack）→ 手动 `git update-ref refs/heads/main <sha>`（此环境 fetch/ref 写入不稳定，refs/remotes 不持久，需手动建 ref）→ `git reset --mixed`
- **提交**：a5478a6（W0 评测体系 + W1 Observability，覆盖 7-04 后全部本地工作），工作区干净
- 注意：此环境 git refs/remotes 写入会丢失（沙箱），push 用 `git push -u origin main` 重建跟踪
- 大文件已跟踪：output/eval/results_*.jsonl（~7MB）、echarts.min.js（1MB）；docker/langfuse/.env 由 .gitignore 排除 ✓
